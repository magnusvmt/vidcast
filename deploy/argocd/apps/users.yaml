apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: users
  namespace: platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/magnusvmt/vidcast.git
    targetRevision: main
    path: deploy/charts/users
    helm:
      # See echo.yaml for why this overrides the chart's own values.yaml.
      valuesObject:
        image:
          repository: ghcr.io/magnusvmt/vidcast/users
          tag: 00a31d1969d949c1cb0979ddea0b93ec51949156
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
