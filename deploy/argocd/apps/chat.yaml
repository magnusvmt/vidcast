apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: chat
  namespace: platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/magnusvmt/vidcast.git
    targetRevision: main
    path: deploy/charts/chat
    helm:
      # See echo.yaml for why this overrides the chart's own values.yaml.
      valuesObject:
        image:
          repository: ghcr.io/magnusvmt/vidcast/chat
          tag: e27554f60cbac54a26eae5b5202c0f0e25cf4dd6
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
