fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pydantic-settings==2.7.1
email-validator==2.2.0
passlib==1.7.4
# Pinned below latest: passlib==1.7.4 reads bcrypt.__about__.__version__, which was
# removed in bcrypt>=4.0. Don't bump this alone — pair it with a passlib upgrade
# (or migration off passlib) or password hashing breaks at import/runtime.
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
